# Typescript node project for playing around with Circles SDK

### Getting started
```
yarn install
```

### How to query data
```
yarn tsx src/query_data.ts
```

We used `tsx` instead of `ts-node` due to continued problems with ESM package imports.